import * as React from 'react';
import { Create } from './Create';
import { Delete, DeleteSelected, DeleteShowing } from './Delete';
import { Update, UpdateSelected, UpdateShowing } from './Update';
import { Show } from './Show';

export const Buttons = {
  Create: Create,
  Delete: Delete,
  DeleteSelected: DeleteSelected,
  DeleteShowing: DeleteShowing,
  Show: Show,
  Update: Update,
  UpdateSelected: UpdateSelected,
  UpdateShowing: UpdateShowing,
};
